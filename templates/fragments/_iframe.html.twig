{% if service|lower == 'youtube' %}
    {% set address  = 'www.youtube.com/embed/' %}
{% elseif service|lower == 'vimeo' %}
    {% set address = 'player.vimeo.com/video/' %}
{% elseif service|lower == 'dailymotion' %}
    {% set address = 'www.dailymotion.com/embed/video/' %}
{% endif %}

<iframe width="{{ width }}" height="{{ height }}" src="https://{{ address }}{{ code }}" title="Vidéo pour illustrer le trick de snowboard {{ trick.name }}"></iframe>
